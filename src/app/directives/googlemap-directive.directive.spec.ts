/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GooglemapDirectiveDirective } from './googlemap-directive.directive';

describe('GooglemapDirectiveDirective', () => {
  it('should create an instance', () => {
    let directive = new GooglemapDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
